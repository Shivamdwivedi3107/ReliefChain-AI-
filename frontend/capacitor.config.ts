import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivam.reliefchainai',
  appName: 'ReliefChain AI',
  webDir: 'dist'
};

export default config;
